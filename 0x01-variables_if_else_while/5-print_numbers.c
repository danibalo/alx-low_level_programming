#include <stdio.h>
/**
 * main -Prints prints all single digit numbers of base 10 starting from 0.
 *Return: always success
 */
int main(void)
{
	int a = 0;

	while (a < 10)
	{
		printf("%d ", a);
		a++;
	}
	return (0);
}

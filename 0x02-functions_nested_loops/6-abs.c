#include "main.h"
/**
 *_abs - computes absolute value of integer
 *@int: integer type is the input for absolute value
 *Return: zero
 */
int _abs(int)
{
	
	int m = -1 * int;

	if (int > 0)
	{
		_putchar(int);
	}
	else if (int == 0)
	{
		_putchar(int);
	}
	else if (n < 0)
	{
		_putchar(m);
	}
	return (0);
}

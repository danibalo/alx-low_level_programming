#include "main.h"
/**
 *print_last_digit - prints last digit of a number
 *@n: the last digit of n will be computed
 *Return: last_digit
 */
int print_last_digit(int n)
{
	int  last_digit = n % 10;

	return (last_digit);
}

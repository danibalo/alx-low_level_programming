#include "main.h"
/**
 *_islower-checks for lowercase character.
 *
 *Return: 0
 */
int _islower(int c)
{
	int c;
	if (islower(c))


	{
		printf("%d is lower case \n", 1, islower(c));
	}
	else
	{
	       	printf("%d is lower case \n", 0);
	}
}


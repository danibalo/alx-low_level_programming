#include "main.h"
/**
 *_is lower - tests the lowest character
 *@c - to test
 *Return: 1 or 0
 * returns 1 if it is lower case
 * returns 0 if it is not
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	
		return (1);
	
	else

		return (0);
	
}

